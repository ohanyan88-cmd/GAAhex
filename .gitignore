__pycache__/
*.pyc
.venv/
.env
node_modules/
dist/
.pytest_cache/
*.egg-info/
.DS_Store
brand/
TASKS/

*storybook.log
storybook-static
backend/boot-test.log
backend/*.log
**/dev.log

# Session debris — auth artifacts (SECRETS — never commit)
token.txt
cookies*.txt
UsersAdminDesktopPortalcookies*.txt
auth_temp.json

# Session debris — boot logs + temp JSON dumps
backend/boot*.err
backend/boot*.out
openapi_temp.json
r_*.json
verify_*_output.txt

# Session debris — one-off scripts (specific names, NOT wildcards — a previous
# `test_p*.py` pattern silently swallowed test_proration.py + test_product_versioning.py).
gen_report.py
gen_rpt.py
test_p45.py
test_p67.py
write_report.py
write_rpt*.py

# pre-cutover forensic pg_dumps — kept on disk for safety, not in history
backend/dev-backup-*.sql
