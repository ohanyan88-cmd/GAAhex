"""Backend service modules — pure-logic helpers that the routers compose.

Every module in this package is import-safe (no DB or app context required at import-time).
Add new helpers as their own file; keep the package surface intentionally small.
"""
