# Architecture Drift Report

**Date**: 2026-06-04
**Repo HEAD**: `70fd44b`
**Mode**: code inspection only — no files modified
**Scope**: `backend/app/`, `frontend/src/`, `frontend-portal/src/`, `backend/tests/`, `backend/scripts/`
**Methodology**: 5 parallel audit packs (backend business logic, backend infrastructure, frontend API/state, frontend UI systems, cross-cutting) synthesized into this consolidated matrix.

Maintenance cost: **H** = wrong customer-visible behavior or financial correctness risk · **M** = requires N-place updates for 1 logical change · **L** = cosmetic, unlikely to diverge

---

## 1. Duplicate Business Logic

| # | Duplication | Canonical (file:line) | Duplicate instances (file:line) | Files | Consolidation recommendation | Cost |
|---|---|---|---|---:|---|---|
| BL-1 | **Invoice `balance_due` — credit-note offset missing in 5 of 6 callers.** Only `services/payment_allocation.py:outstanding_for_invoice()` subtracts both allocations AND applied credit notes. Every other caller computes only `SUM(Payment.amount)`, so a customer with an applied credit note sees a wrong (too-high) balance in portal + invoice HTML + documents. | `services/payment_allocation.py:50–73` (`outstanding_for_invoice`) | `routers/billing_invoice.py:231–234` · `routers/portal_billing.py:43–47` (`_paid_total`) · `routers/portal.py:50–55` · `routers/payment_gateway.py:143–146` · `routers/documents.py:146–148` | 6 | All 5 duplicate callers call `outstanding_for_invoice(s, inv.id)` instead of recomputing the SUM | **H** |
| BL-2 | **`amd()` money formatter with inconsistent rounding.** Three independent `def amd` definitions; the version in `ai.py` uses `{:,.0f}` (zero decimal places) while the others use `{:,.2f}` — AI monetary summaries silently show rounded whole-dram figures. | `routers/documents.py:45–47` (`_amd`) | `routers/portal_billing.py:150–151` (local def inside `invoice_document`) · `routers/portal_billing.py:288–289` (second local def inside `payment_receipt`) · `routers/ai.py:31–33` (`_amd`, different rounding) | 4 | Extract to `app/utils/money.py:amd_format(luma) -> str` (2 d.p.); import everywhere; delete local defs | **H** |
| BL-3 | **Invoice PAID auto-flip divergence.** The `add_payment` legacy path uses raw `SUM(amount − refunded_amount) ≥ total` — correctly accounts for refunds but ignores credit notes. The allocation service uses `outstanding_for_invoice` which deducts both. When both payments and credit notes exist, the two paths disagree on when an invoice flips to PAID. | `services/payment_allocation.py:161–163` | `routers/billing_payment.py:175–181` | 2 | Update `billing_payment.py:175–181` to call `outstanding_for_invoice` | **H** |
| BL-4 | **`communications.py` COUNT+1 reference number — non-atomic race.** The router defines its own `_next_reference_number` that reads `SELECT COUNT(*) FROM Communication WHERE tenant_id=…` then adds 1, with a 5-iteration `IntegrityError` retry loop. Under contention the loop can exhaust (5 attempts → HTTP 500). The SEQUENCE-backed `utils/refnum.py:next_reference_number()` already exists and eliminates the race. A comment at `communications.py:17,95` explicitly labels it legacy. | `utils/refnum.py:73–112` (`next_reference_number`) | `routers/communications.py:94–105` (COUNT+1 with retry loop) | 2 | Replace with `next_reference_number(s, tenant_id, prefix="COM", width=6)` | **H** |
| BL-5 | **`_parse_dt` ISO-8601 parser: tz-naive coercion missing in 16+ copies.** `_billing_shared._parse_dt` applies `.replace(tzinfo=timezone.utc)` when the parsed datetime is tz-naive. The 16+ inline or per-router copies do NOT apply this coercion, so a client sending `2026-01-15T08:00:00` (no Z) produces a tz-naive `datetime` that raises `TypeError: can't compare offset-naive and offset-aware datetimes` on the first comparison. | `routers/_billing_shared.py:325–344` (`_parse_dt`) | `routers/calendar.py:50–62` · `routers/workitems.py:56–68` · `routers/tasks.py:346–357` · `routers/noc_inventory.py:797–816` · `routers/documents.py:58–64` · `routers/jobs.py:82–90` · `routers/audit_log.py:41` · `routers/apikeys.py:95` · `routers/webhooks.py:320` · `routers/portal_service.py:112,117` · `routers/relationships.py:138` · `routers/imports_exports.py:467` · `routers/slas.py:228,238` · `routers/report_schedules.py:166,222,321` · `routers/revenue_assurance.py:108` | 17+ | Export to `app/utils/dt.py:parse_iso_dt(value, field, optional=False)`; import everywhere; delete local copies | **H** |
| BL-6 | **`BILLED_STATUSES` constant defined twice.** Same values `("ISSUED","OVERDUE","PAID")` defined independently — a future status addition would require 2-place update. | `services/account_balance.py:43` `_BILLED_STATUSES` | `routers/customer360.py:42` `BILLED_STATUSES` | 2 | Move to `routers/_billing_shared.py` or `app/utils/billing_constants.py`; import in both | **M** |
| BL-7 | **`s.add(Event(...))` bypass of `workflow.emit()`.** Four routers hand-roll Event rows directly, skipping `event_name`, `category`, `schema_version`, `actor_type`, `visibility` fields that `workflow.emit` populates — producing structurally under-specified audit events. | `workflow.py:69` (`workflow.emit`) | `routers/comm.py:138–141` · `routers/feature_flags.py:156–167` · `routers/page_bindings.py:141–153` · `routers/page_bindings.py:179–189` | 5 | Replace the 4 non-intentional bypasses with `workflow.emit()`. The `feature_gate.py` bypass is intentional (separate session lifetime) — keep with comment. | **M** |
| BL-8 | **`outstanding_for_invoice` formula reimplemented in GET endpoint.** The GET `/invoices/{id}/outstanding` endpoint in `billing_invoice.py:303–331` re-computes the same formula as the service function instead of calling it. | `services/payment_allocation.py:49–73` | `routers/billing_invoice.py:303–331` | 2 | Call `outstanding_for_invoice(s, inv.id)` directly | **M** |
| BL-9 | **`_now()` / `_iso()` datetime helpers defined 30+ times.** Consistent logic but pollutes grep output and forces N-place updates if the idiom ever changes. | `routers/_billing_shared.py:92–94` (`_now`), `L:167–168` (`_iso`) | Both helpers re-defined at the top of 25+ router files and 6+ service files | 30+ | Export both to `app/utils/dt.py`; replace local defs with `from app.utils.dt import now_utc, iso_format` | **L** |
| BL-10 | **`_deny(perm)` permission-denial helper duplicated in 16 files.** Identical one-liner `raise HTTPException(403, {"error":"forbidden","required_permission":perm})`. | `routers/_billing_shared.py:62–63` | `routers/accounts.py:43` · `routers/dunning.py:46` · `routers/helpdesk.py:59` · `routers/noc_dashboard.py:80` · `routers/noc_inventory.py:49` · `routers/payment_methods.py:45` · `routers/payment_gateway.py:45` · `routers/revenue_assurance.py:44` · `routers/respool.py:32` · `routers/credit_notes.py:46` · `routers/install_board.py:48` · `routers/services.py:42` · `routers/tariff_plans.py:40` · `routers/usage.py:33` · `routers/workitems.py:44` · `routers/records.py:178` | 17 | Move to `app/utils/http_errors.py`; import in all 16 files | **L** |
| BL-11 | **Seed scripts bypass service layer.** `scripts/seed_churn_data.py` and `scripts/seed_dashboard_data.py` write `Invoice`, `Subscription`, `Payment`, `WorkItem` rows directly via ORM, re-implementing invoice numbering (`f"INV-{n+1:05d}"`), subscription billing, and payment creation inline. Any billing rule change silently diverges the seed output. | `routers/billing_invoice.py`, `billing_subscription.py`, `billing_payment.py` (service layer) | `scripts/seed_churn_data.py:84+` · `scripts/seed_dashboard_data.py:74+` | 4 | Refactor scripts to call the HTTP API or extract `create_invoice_for_seed(s, ...)` service functions | **H** |

---

## 2. Duplicate API Clients

| # | Duplication | Canonical (file:line) | Duplicate instances (file:line) | Files | Consolidation recommendation | Cost |
|---|---|---|---|---:|---|---|
| AC-1 | **`authH` Bearer header factory defined privately in 32 admin-frontend files.** `lib/billing.ts:6` exports it; `lib/api.ts:3` defines it privately; 30 view/component files define their own `const authH = (t: string) => ({Authorization: 'Bearer ' + t})` locally. No view imports it from either lib file. | `frontend/src/lib/billing.ts:6` (exported) | 30 view/component files each defining their own local `authH` — including `DashboardView.tsx:29`, `HomeView.tsx:32`, `EntityView.tsx:55`, `MessagesView.tsx:20`, plus 8 studio panes and 2 components | 32 | Delete all 30 local declarations; import from `lib/billing.ts` | **H** |
| AC-2 | **57 raw `fetch(${BASE}/…)` calls in 18 view files bypass the `bget`/`bpost` wrapper.** `lib/billing.ts` provides `bget`/`bpost`/`bpatch`/`bdel` wrappers with error normalisation; 18 views call `fetch()` directly, getting no error normalisation and no consistent response unwrapping. | `frontend/src/lib/billing.ts` (`bget`/`bpost`) | `DashboardView.tsx` (25 raw fetches) · `CalendarView.tsx` (4) · `MessagesView.tsx` (4) · `EntityView.tsx` (7) · `HomeView.tsx` (1) + 13 more view files | 18 | Route all reads through `bget`; create domain-specific wrappers (`analytics.ts`, `noc.ts`) if needed | **H** |
| AC-3 | **No centralized 401 handler in admin frontend.** Portal `lib/api.ts:25–28` centralizes: 401 → `clearToken()` + redirect `/login`. Admin has no equivalent — every fetch caller handles (or ignores) 401 independently. On token expiry, admin users may see a broken UI rather than a login redirect. | `frontend-portal/src/lib/api.ts:25–28` (portal canonical) | Admin `lib/billing.ts` and 18 direct-fetch views — zero centralized 401 intercept | all admin fetch sites | Add `authedFetch(token, path, opts)` thin wrapper that auto-injects the header and intercepts 401 → logout + redirect | **H** |
| AC-4 | **Portal duplicates admin API client architecture independently.** Portal `lib/api.ts` is a self-contained fetch wrapper with `getToken`/`setToken`/`clearToken` (localStorage) and all portal endpoints inline. Admin uses `lib/billing.ts` `bget`/`bpost` with prop-drilled bearer. Two completely different auth persistence strategies: portal persists to localStorage; admin holds token only in React state (no persistence across page refresh). | `frontend-portal/src/lib/api.ts:1–180` | `frontend/src/lib/billing.ts:1–200` + `lib/api.ts:1–50` | 2 | Extract shared `@gaahex/http-client` package or at minimum share 401-intercept logic, token refresh, and response error shape | **M** |
| AC-5 | **8 bare `httpx.AsyncClient(timeout=X)` instantiations with no factory.** Each integration rolls its own client construction — no shared retry/timeout/TLS defaults. | — (no canonical factory exists) | `routers/ai.py:74,93` · `routers/channels.py:274` · `adapters/payment/arca.py:77,107` · `adapters/sms.py:121` · `routers/webhooks.py:142` · `services/workflow.py:428` | 5 | Create `backend/app/http_client.py:get_async_client(timeout)` factory; all sites import it | **H** |

---

## 3. Duplicate Validation

| # | Duplication | Canonical (file:line) | Duplicate instances (file:line) | Files | Consolidation recommendation | Cost |
|---|---|---|---|---:|---|---|
| VA-1 | **Frontend form validation: no shared library.** No react-hook-form, Formik, or Zod usage anywhere. Every form uses ad-hoc `useState` per field + per-submit validation. Email/phone/date field validation is re-implemented per-form across the codebase. | — (no canonical) | Every form-containing god file — `EntitiesPane.tsx`, `NotificationsPane.tsx`, `WebhooksPane.tsx`, `OrdersView.tsx`, `ServicesView.tsx`, `CustomerView.tsx`, `InvoicesView.tsx`, etc. | ~35 forms | Adopt `react-hook-form` with a shared `validators.ts` (`validateEmail`, `validatePhone`, `validateDateRange`, `validateAmount`) | **H** |
| VA-2 | **`_parse_decimal_opt` helper duplicated in 3 routers.** Same `Decimal(str(v))` coercion with `None` guard. | `routers/_billing_shared.py:39–46` (`_parse_decimal_opt`) | `routers/orders.py:458–466` · `routers/credit_notes.py:88–92` | 3 | Consolidate into `_billing_shared.py`; import in both | **L** |
| VA-3 | **Payment method type validation as set literal duplicated.** `billing_payment.py` validates `method in {"cash","card","transfer"}` inline. Frontend `billing.ts:52` has the same values as a comment string only. No shared constant. | `routers/billing_payment.py` (set literal) | `frontend/src/lib/billing.ts:52` (comment) | 2 | Extract to a `PaymentMethod` const array in `billing.ts`; import into backend via `app/utils/billing_constants.py` | **M** |
| VA-4 | **Server-error rendering per form.** No canonical "show field errors from 422 response" helper. Each form has its own `catch (err) → setError(err.message)` or similar, losing structured field-level errors from FastAPI's 422 detail array. | — (no canonical) | 6 views with explicit try/catch: `OrgView.tsx`, `EntityView.tsx`, `HomeView.tsx`, `CollectionsView.tsx`, `WebhooksView.tsx`, `MyTasksView.tsx` | 6 | Extract `handleMutationError(err, fallback)` that parses 422 detail + calls `toast.error`; adopt in all mutation callers | **M** |
| VA-5 | **No TypeScript enum enforcement on status fields.** Backend emits `"DRAFT"`, `"ISSUED"`, `"PAID"`, `"OVERDUE"`, `"VOID"` etc. Frontend TS types use `string | null?` — any typo silently compiles. `WorkItem` status is the one exception: `workitems.ts:8` defines a correct TS union type. | `frontend/src/lib/workitems.ts:8–9` (correct pattern) | `billing.ts` `Invoice.status: string | null?` · `Subscription.status: string | null?` · all other entity types | 10+ entity types | Introduce `InvoiceStatus = "DRAFT" | "ISSUED" | "PAID" | "OVERDUE" | "VOID"` union types across all entity interfaces; or generate from `docs/standards/14-enum-registry.md` | **M** |

---

## 4. Duplicate State Management

| # | Duplication | Canonical (file:line) | Duplicate instances (file:line) | Files | Consolidation recommendation | Cost |
|---|---|---|---|---:|---|---|
| SM-1 | **`token` prop-drilled to all 55+ views.** The access token lives in `App.tsx:165` `useState` and is passed as a prop to every single view. No Auth context, no Zustand store. Every view function signature starts with `token: string`. | `App.tsx:165` (source) | All 55+ view files | 55+ | Move token to a React context (`AuthContext`) or Zustand store; eliminate the prop thread entirely | **H** |
| SM-2 | **`capabilities` re-fetched in 5 views despite being passed as prop.** `App.tsx:176` fetches once on login and passes `capabilities` as a prop. Five views (`DashboardView:730`, `HomeView:268`, `NetworkInventoryView:187`, `OrdersView:174`, `RevenueAssuranceView:163`) ignore the prop and create a second local fetch — 5 redundant network round-trips on every view mount. | `App.tsx:176` + prop chain | `views/DashboardView.tsx:730` · `views/HomeView.tsx:268` · `views/NetworkInventoryView.tsx:187` · `views/OrdersView.tsx:174` · `views/RevenueAssuranceView.tsx:163` | 5 + App | Remove `fetchCapabilities` calls from those 5 views; trust the prop (or lift to Context) | **H** |
| SM-3 | **Admin app: no persistence of token across page refresh.** Token in `App.tsx:165` is only in React state — cleared on every page refresh. Portal uses `localStorage` (`portal-token`). Two completely different session persistence strategies; admin forces re-login after every browser reload. | `frontend-portal/src/lib/api.ts:2–13` (localStorage) | `frontend/src/App.tsx:165` (ephemeral state) | 2 | Pick one strategy; document it. Add `sessionStorage` or `localStorage` with HttpOnly-fallback for admin token if persistent sessions are desired. | **M** |
| SM-4 | **Two feature-flag systems with no bridge.** Backend has static boot-time kill-switches (`config.py:133–136` `feature_radius_required`, `feature_olt_provisioning_required`, etc.). Frontend has DB-backed runtime flags via `/api/feature-flags` (`frontend/src/lib/useFlag.ts:66`). No mechanism bridges them — a `feature_radius_required=false` boot flag does not automatically disable the corresponding DB feature-flag. | `backend/app/config.py:133–136` (infra kill-switch) | `frontend/src/lib/useFlag.ts:66` (runtime DB flags) | 2 | Document the two tiers: boot flags = infra kill-switch (hard no); DB flags = tenant UX switches. Add a `FEATURE_*` boot-flag → DB flag sync check on startup | **M** |
| SM-5 | **Startup seed calls duplicated between `main.py` and `conftest.py`.** `main.py:lifespan` and `conftest.py:86–91` both call the same 4 seed functions (`seed_if_empty`, `seed_meta_if_empty`, `seed_access_if_empty`, `seed_demo_loop_if_empty`). Any new seed function added to one must be manually added to the other. | `backend/app/main.py:lifespan` | `backend/tests/conftest.py:86–91` | 2 | Extract `async def apply_seeds(engine)` function; call from both | **M** |

---

## 5. Duplicate Permissions Checks

| # | Duplication | Canonical (file:line) | Duplicate instances (file:line) | Files | Consolidation recommendation | Cost |
|---|---|---|---|---:|---|---|
| PC-1 | **`_deny(perm)` permission-denial helper duplicated 16 times.** (See BL-10 above — also a permissions concern.) | `routers/_billing_shared.py:62–63` | 16 router files | 17 | Covered in BL-10 | **L** |
| PC-2 | **`approval_required` HTTP 202 response shape duplicated 9+ times.** Every router that implements an approval gate raises `HTTPException(202, detail={"status":"approval_required","approval_id":...,"action_type":...})` inline — no helper. If the shape ever gains a new field (e.g. `message`), all 9+ sites must be updated. | `routers/records.py:496` (earliest/fullest instance) | `billing_invoice.py:177,540` · `billing_credit_note.py:137` · `billing_payment.py:154,276` · `billing_subscription.py:187` · `assets.py:157` · `procurement.py:158` · `roles.py:163` · `services.py:455` | 9+ | Extract `approval_required(approval_id, action_type) -> HTTPException` in `app/utils/http_errors.py`; import everywhere | **M** |
| PC-3 | **Backend RBAC permission key strings not mirrored as TS constants on frontend.** Backend `seed.py` hardcodes 100+ permission key strings (`"invoice.manage"`, `"service.activate"`, etc.) in `_permission_specs()`. Frontend `action-menu.ts:27` uses `permissionKey: string` with no shared constants file — permission strings are scattered as comments and string literals in each component. Any rename requires grep-driven multi-file update. | `backend/app/seed.py:_permission_specs()` + `docs/standards/15-permission-registry.md` | `frontend/src/lib/capabilities.ts` (generic `{view,create,edit,delete}` shape, no key constants) | 60+ (any component that gates on a permission string) | Generate `frontend/src/generated/permissions.ts` with `export const Perms = { invoice: { manage: "invoice.manage" }, ... }` from the permission registry at build time | **M** |
| PC-4 | **Frontend capabilities re-fetched in 5 views** (overlaps SM-2). Permissions are sourced from `fetchCapabilities` which calls `GET /api/me/capabilities`. 5 views fetch independently despite receiving the result as a prop. | `App.tsx:176` | 5 views (see SM-2) | 5 | See SM-2 — same fix | **H** |

---

## 6. Duplicate Data Fetching

| # | Duplication | Canonical (file:line) | Duplicate instances (file:line) | Files | Consolidation recommendation | Cost |
|---|---|---|---|---:|---|---|
| DF-1 | **`useEffect + fetch + setState` loading pattern — no server-state caching.** Zero React Query / SWR usage. Every view implements its own loading state, error state, refetch logic, and stale handling inline. The same entity (e.g. customer invoices) is fetched independently by multiple components on the same screen. | — (no canonical hook) | All 18 views with direct `fetch` calls + ~35 more via `bget` in `useEffect` | all views | Adopt `@tanstack/react-query`; extract `useInvoices(token, customerId)`, `useCustomer(token, id)`, etc.; server-state deduplication and cache come for free | **H** |
| DF-2 | **`let alive = true; return () => { alive = false }` cleanup idiom duplicated 78 times.** Every `useEffect` that fetches data independently manages its own `alive` guard to prevent setState-after-unmount. | — (no canonical wrapper) | `DashboardView.tsx` (23 pairs) · `RevenueAssuranceView.tsx` (5) · `NetworkInventoryView.tsx` (2) · `OrdersView.tsx` (2) · `SchedulingView.tsx` (2) + ~20 other files | ~20 files, 78 pairs | Encapsulate in a `useFetch<T>(url, token)` hook that handles the `alive` guard, loading/error state, and returns `{data, loading, error, refetch}` | **H** |
| DF-3 | **Backend pagination helper adoption gap: 12+ routers hand-roll `LIMIT/OFFSET`.** `backend/app/pagination.py` provides `Page`, `paginate_select`, `count_select` with canonical `DEFAULT_LIMIT=100`, `MAX_LIMIT=1000`. 12+ routers ignore it and write their own `.offset().limit()` with local `DEFAULT_LIMIT`/`MAX_LIMIT` constants (some differ: `audit_log.py` uses 50/500; `search.py` uses 20). | `backend/app/pagination.py:44–60` (`Page`, `paginate_select`) | `routers/audit_log.py:30–31` · `routers/dunning.py:169` · `routers/credit_notes.py:139` · `routers/install_board.py:319` · `routers/noc_dashboard.py:240,581,915` · `routers/noc_inventory.py:187,420,553,581,692` · `routers/revenue_assurance.py:224,332` · `routers/payment_methods.py:284` · `routers/search.py:201` | 12+ | Replace every hand-rolled `q.offset().limit()` block with `Page.from_request(limit, offset).apply(q)`; delete local `DEFAULT_LIMIT`/`MAX_LIMIT` re-declarations | **H** |
| DF-4 | **`fmtDate(iso)` defined privately in 15 view files.** No canonical date helper in `frontend/src/lib/`. `lib/time.ts` exports only `timeAgo`. All 15 private definitions are identical or near-identical (`new Date(iso).toLocaleDateString()`). | — (no canonical export; should be `frontend/src/lib/time.ts`) | `views/InvoicesView.tsx:51` · `views/CustomerView.tsx:114` · `views/PaymentsView.tsx:18` · `views/OrdersView.tsx:88` · `views/HelpdeskView.tsx:26` · `views/WorkItemsView.tsx:34` · `views/RevenueAssuranceView.tsx:130` · `views/NetworkInventoryView.tsx:147` · `views/ServicesView.tsx:33` · `views/MyTasksView.tsx:75` · `views/CustomerTasksView.tsx:86` · `views/PaymentGatewayView.tsx:18` · `views/ProvisioningView.tsx:18` + 2 more | 15 | Add `export function fmtDate(iso: string | null): string` to `lib/time.ts`; replace 15 private defs | **H** |
| DF-5 | **`fmtDateTime` defined privately in all 6 `customer-tabs/` files.** All identical: `new Date(iso).toLocaleString()`. | — | `customer-tabs/ApprovalsTab.tsx:22` · `CommentsTab.tsx:18` · `AttachmentsTab.tsx:21` · `AuditTab.tsx:21` · `TimelineTab.tsx:20` · `CommunicationsTab.tsx:22` | 6 | Add `export function fmtDateTime(iso: string | null): string` to `lib/time.ts`; replace 6 private defs | **M** |
| DF-6 | **`moneyDecimal(s)` / `moneyDec(s)` defined privately in 3 view files.** Converts a Decimal server-string (`"1234.50"`) to a formatted AMD display. Three private copies with slightly different null-handling. | `frontend/src/lib/money.ts:3` (`money(luma)` exists; `moneyDecStr` doesn't) | `views/AccountsView.tsx:61` (`moneyDecimal`) · `views/CustomerView.tsx:135` (`moneyDecimal`) · `views/InvoicesView.tsx:29` (`moneyDec`) | 3 | Add `export function moneyDecStr(s: string | null): string` to `lib/money.ts`; replace 3 private defs | **M** |
| DF-7 | **Portal `fmt(luma)` defined privately in all 3 money-rendering portal views.** Uses `hy-AM` locale vs admin's `en-US`. Three identical private closures; no `frontend-portal/src/lib/money.ts` file exists. | — (no canonical) | `portal/views/DashboardView.tsx:4` · `portal/views/BillsView.tsx:15` · `portal/views/ServiceView.tsx:15` | 3 | Create `frontend-portal/src/lib/money.ts` with `export function fmt(luma: number): string`; import in all 3 views | **M** |
| DF-8 | **No OpenAPI-to-TS type generation.** All TypeScript entity types are hand-mirrored from backend dict serializers. Backend returns raw `dict` from almost every endpoint; no Pydantic `response_model`. Type drift is silent: 5 audited entities show missing fields in TS (`Invoice` missing `owner_node_id`, `posted_at`, `account_id`; `Subscription` missing `account_id`, `billing_anchor_day`; `Service` and `Order` have no canonical TS type at all). | — (no codegen) | `frontend/src/lib/billing.ts` (all types) · all `lib/*.ts` type files | 60+ entity type files | Adopt `openapi-typescript` codegen; backend emits OpenAPI schema; frontend types become single-source auto-generated | **H** |

---

## 7. Duplicate Modal Systems

| # | Duplication | Canonical (file:line) | Duplicate instances (file:line) | Files | Focus trap? | Esc? | ARIA? | Cost |
|---|---|---|---|---:|---|---|---|---|
| MO-1 | **Studio `CreateDialog` — 3 hand-rolled `position:fixed,inset:0` form modals.** Same chrome (scrim + centered card + header + form + footer) implemented independently in 3 studio panes. None use `<Modal>` or `<Overlay>`; none have focus traps, Esc handlers, or `aria-modal`. | `components/Modal.tsx:28` + `Overlay.tsx` (canonical) | `studio/EntitiesPane.tsx:181–420` · `studio/NotificationsPane.tsx:146–300` · `studio/WebhooksPane.tsx:147–280` | 3 | Replace with `<Modal>` + `<Overlay>` | NO | NO | NO | **H** |
| MO-2 | **Studio `ConfirmDeleteDialog` — 2 identical hand-rolled confirm dialogs.** Same chrome as MO-1. Both can be replaced with `<Modal>` + the existing `ConfirmHost` pattern. | `components/Modal.tsx ConfirmHost` | `studio/EntitiesPane.tsx:425–466` · `studio/NotificationsPane.tsx:300–344` | 2 | Replace with `ConfirmHost` or `<Modal>`; `ConfirmHost` already handles the pattern | NO | NO | NO | **H** |
| MO-3 | **Studio `ConfirmTestSendDialog` — 1 hand-rolled test-send confirm.** Same chrome; `NotificationsPane.tsx:350–410`. | `components/Modal.tsx` | `studio/NotificationsPane.tsx:350–410` | 1 | Replace with `<Modal>` | NO | NO | NO | **M** |
| MO-4 | **`ChartPicker` — hand-rolled `position:fixed,inset:0` picker overlay.** No `<Modal>` or `<Overlay>` usage; no focus trap, no `aria-modal`. | `components/Modal.tsx` | `components/ChartPicker.tsx:39–80` | 1 | Replace with `<Modal>` or convert to popover | NO | NO | NO | **M** |
| MO-5 | **`ConfigureDrawer` — hand-rolled `position:fixed` chrome with `useFocusTrap` but no Esc on backdrop.** Uses `useFocusTrap` so trap-Esc fires `onClose`; but the backdrop div has no `keydown` handler (misses the trap-skip case). | `components/Modal.tsx` / `Overlay.tsx` | `modals/ConfigureDrawer.tsx:164–332` | 1 | Wrap in `<Overlay>` to get body-scroll-lock + consistent Esc semantics | YES (via trap) | PARTIAL | YES | **L** |
| MO-6 | **`ConfirmModal` footer button pair repeated verbatim in every modal.**`<button className="btn btn-ghost btn-md">Cancel</button><button className="btn btn-primary btn-md">…</button>` copy-pasted across all modal call sites. Even `ConfirmHost` in `Modal.tsx:146` bypasses `<Button>` primitive. | — (no helper) | All 12+ modal callers | 12+ | Extract `<ModalFooterActions onCancel onConfirm label />` helper | n/a | n/a | n/a | **M** |

---

## 8. Duplicate Drawer Systems

| # | Duplication | Canonical (file:line) | Duplicate instances (file:line) | Files | Focus trap? | Esc? | ARIA? | Cost |
|---|---|---|---|---:|---|---|---|---|
| DR-1 | **Studio `DrawerShell` — 3 verbatim copies of the same hand-rolled slide-out chrome.** Each studio pane (`EntitiesPane`, `WebhooksPane`, `NotificationsPane`) defines its own `DrawerShell`-style component: `position:fixed, inset:0` scrim + right-anchored panel + header + scrollable body. None use `RecordDrawer` or `SlideOutPanel`. No focus trap, no Esc handler, no ARIA. | `components/RecordDrawer.tsx:91` (canonical) | `studio/EntitiesPane.tsx:970–1004` · `studio/WebhooksPane.tsx:711–745` · `studio/NotificationsPane.tsx:782–820` | 3 | Build shared `<StudioDrawer>` component wrapping `RecordDrawer` or `Overlay`; delete 3 copies | NO | NO | NO | **H** |
| DR-2 | **`RecordDrawer` has no focus trap — only partial focus management.** Focus is moved into the drawer on open (`drawerRef.current?.focus()`) but there is no roving tab-key trap. Tab-key exits the drawer into the underlying page. | `components/RecordDrawer.tsx:91` | — (the canonical itself is incomplete) | 1 | Add `useFocusTrap(drawerRef, { onClose })` — same pattern already used by `ConfigureDrawer` and `Overlay` | PARTIAL | YES | YES | **H** |
| DR-3 | **`SlideOutPanel` (NMS-specific) has no focus trap.** Shares Esc and ARIA (`role=dialog`, `aria-modal`, `aria-hidden`) but no `useFocusTrap`. | `page-shell/SlideOutPanel.tsx:28` | — (the component itself is incomplete) | 1 | Add `useFocusTrap` | NO | YES | YES | **M** |
| DR-4 | **4 distinct drawer flavors with inconsistent capabilities.** The platform has `RecordDrawer` (partial focus), `SlideOutPanel` (NMS, no trap), `ConfigureDrawer` (full useFocusTrap, Esc, ARIA), and 3 studio `DrawerShell` copies (nothing). No single "gold standard" drawer. | `modals/ConfigureDrawer.tsx:164` (fullest A11y) | See DR-1, DR-2, DR-3 | 6 files | Consolidate to: `ConfigureDrawer` pattern → canonical; `RecordDrawer` + `SlideOutPanel` get `useFocusTrap`; studio panes use new `<StudioDrawer>` | VARIES | VARIES | VARIES | **H** |

---

## 9. Duplicate Tab Systems

| # | Duplication | Canonical (file:line) | Duplicate instances (file:line) | Files | aria-selected? | Keyboard nav? | Cost |
|---|---|---|---|---:|---|---|---|
| TB-1 | **10 distinct tab flavors — no canonical `<TabButton>` primitive.** At least: (A) `.tab/.tab.on` pill CSS · (B) `.drawer-tab` underline CSS · (C) `CustomerTabButton` with count badge · (D) `InvoiceTabButton`/`AccountTabButton` (no badge) · (E) `RaTabButton`/`NiTab`/`TabButton` (with subtitle) · (F) `.tab.on` pill inside ConfigureDrawer · (G) studio `.tabs` pill · (H) `org-switcher-btn` segmented · (I) `ps-demo-tab` (incorrectly uses `aria-pressed` instead of `aria-selected`) · (J) `Zone2Tabs` renderer | — (no canonical `<TabButton>` primitive — see tokenization audit C-5) | See full flavor table in Pack D audit | 14+ | Build one `<DetailTab>` (underline + optional count + optional subtitle) and one `<PillTab>` primitive; replace all inline tab button implementations | VARIES | **NO anywhere** | **H** |
| TB-2 | **`InvoiceTabButton` and `AccountTabButton` are identical.** Both render: azure 2px underline on active, `role="tab"`, `aria-selected`, no count badge, no subtitle. Copy-pasted between the two views. | `views/InvoicesView.tsx:401–432` (`InvoiceTabButton`) | `views/AccountsView.tsx:471–502` (`AccountTabButton`) | 2 | Single `<DetailTab>` replaces both | YES | NO | **M** |
| TB-3 | **No keyboard navigation in ANY hand-rolled tab implementation.** All 7 inline tab button components (flavors C–E) and both CSS systems (A–B) lack arrow-key navigation, Home/End key, and `aria-controls` ↔ `aria-labelledby` binding. WCAG 2.1.1 (keyboard) + 4.1.3 violated. | — | All 14+ tab implementations | all | YES (most) | NO (all) | **H** |
| TB-4 | **9-tab object-detail spec implemented 3 times.** 16 duplicate React tab body components (`InvoiceTimelineTab`, `InvoiceTasksTab`, `InvoiceCommentsTab`, `InvoiceAttachmentsTab`, `InvoiceApprovalsTab`, `InvoiceRelatedTab`, `InvoiceCommunicationsTab`, `InvoiceAuditTab` in `InvoicesView.tsx:695–960`; plus identical set in `AccountsView.tsx:677–960`). | `views/customer-tabs/*.tsx` (9 canonical tab bodies) | `views/InvoicesView.tsx:695–960` (8 duplicates) · `views/AccountsView.tsx:677–960` (8 duplicates) | 3 | Parameterize `views/customer-tabs/*` to accept entity-type + id props; delete 16 duplicate React functions | n/a | n/a | **H** |
| TB-5 | **`PageShellDemoView` uses `aria-pressed` instead of `aria-selected` on tab buttons.** Functional bug in the demo view that developers reference as a pattern example. | ARIA APG tab pattern | `views/PageShellDemoView.tsx:317–328` | 1 | Replace `aria-pressed` with `aria-selected` | NO (bug) | NO | **L** |

---

## 10. Duplicate Table Systems

| # | Duplication | Canonical (file:line) | Duplicate instances (file:line) | Files | Sort? | Filter? | Pagination? | Cost |
|---|---|---|---|---:|---|---|---|---|
| TL-1 | **`<DataTableRow>` / `<DataTableCell>` primitives — zero production callers.** Built with sorting/selection/checkbox/RowActionsMenu support. Every production view uses `<table className="grid">` directly instead. The primitives exist only in Storybook stories. | `primitives/DataTableRow.tsx` · `primitives/DataTableCell.tsx` | ZERO production callers — `stories/*.stories.tsx` only | 0 production | stub | stub | stub | **H** (dead code + missed feature) |
| TL-2 | **Two parallel kanban board implementations.** `WorkItemsBoard.tsx` is the shared component; `LeadPipelineView` and `InstallationBoardView` bypass it and build raw `.kcol/.kcard` HTML directly. All three boards have the same visual structure (swimlane columns + draggable cards) but different codebases. | `components/WorkItemsBoard.tsx` (shared) | `views/LeadPipelineView.tsx:256` (raw `.kcard`) · `views/InstallationBoardView.tsx:242` (raw `.kcol/.kcard`) | 3 | Migrate both views to use `WorkItemsBoard`; extend it with their unique column types | NO | NO | NO | **H** |
| TL-3 | **`<ul>/<li>` lists used for tabular data (timeline/comment tabs) in InvoicesView.** `InvoicesView.tsx:715–729` and `InvoicesView.tsx:794–807` render data tables as `<ul><li>` rather than `<table>` — semantically wrong, inconsistent with every other list view, and breaks `ActivityTimeline.tsx` integration. | `components/ActivityTimeline.tsx` (canonical for timeline rows) | `views/InvoicesView.tsx:715–729` · `views/InvoicesView.tsx:794–807` | 2 | Use `<ActivityTimeline>` for timeline tab; `<SkeletonRows>` + `<table className="grid">` for comments | NO | NO | NO | **M** |
| TL-4 | **Inline action buttons in 15+ table views bypass `RowActionsMenu`.** Inconsistent action-cell UX: some tables use the canonical `<RowActionsMenu>` ⋮ menu; most tables use ad-hoc `<button className="btn btn-ghost btn-sm">` inline in the actions column. | `components/RowActionsMenu.tsx:31` | `views/InvoicesView.tsx:330–334` · `views/CustomerView.tsx:641–642` · `views/CustomerBillingModal.tsx:206–208` + ~12 more views | 15+ | Migrate all row-action cells to `<RowActionsMenu>`; per file 09 standards, row actions live in the last table column via the canonical component | NO | NO | NO | **H** |
| TL-5 | **15+ views hand-roll search/filter inputs instead of `<FilterBar>` zone E.** `<FilterBar>` is adopted by only 9 of 51 PageShell views. 42 views build their own `.toolbar` with raw `<input className="inp" placeholder="Search...">`. No typeahead, no active-filter chip row, no consistent keyboard shortcut. | `page-shell/FilterBar.tsx:19` (zone E) | `InteractionsView.tsx:279` · `PartiesView.tsx:173` · `EntityView.tsx:789` · `NetworkInventoryView.tsx:816+915` · `MessagesView.tsx:233` · `ProductsView.tsx:235` · `OutboundView.tsx:379` · `OrgView.tsx:484+1834` · `PaymentGatewayView.tsx:205` · `ResourcePoolsView.tsx:226` · `WorkItemsView.tsx:289` · `ServicesView.tsx:210` · `UsageView.tsx:181` · `SubscriptionsView.tsx:278` · `WebhooksView.tsx:223` + ~12 more | 15+ | Migrate filter UI to `PageShell filters=` prop; increase `<FilterBar>` adoption from 9 to 51 views | NO | NO | NO | **M** |
| TL-6 | **`_ensure`/`_ensure_user` test fixture duplicated across 18 test files.** Same user-creation + role-assignment setup function copy-pasted with minor password-string differences. Any schema change (e.g., adding `department` to `Assignment`) requires 18 updates. | `backend/tests/conftest.py` (should be here but isn't) | 18 test files — see E3 in Pack E audit | 18 | Move to `conftest.py` as `async def ensure_user(s, ...) -> uuid.UUID` fixture factory | n/a | n/a | n/a | **H** |
| TL-7 | **`_customer(client, admin, name)` test helper duplicated across 27 test files.** Same `POST /api/customers` call + `return r.json()["id"]`. | `backend/tests/conftest.py` (should be here) | 27 test files — see E3 in Pack E audit | 27 | Move to `conftest.py` as `async def make_customer(client, admin, name) -> str` | n/a | n/a | n/a | **H** |

---

## Aggregate Totals

| Group | Findings | Files affected (unique) | H-cost | M-cost | L-cost |
|---|---:|---:|---:|---:|---:|
| 1. Business Logic | 11 | ~40 | 5 | 3 | 3 |
| 2. API Clients | 5 | ~55 | 4 | 1 | 0 |
| 3. Validation | 5 | ~45 | 1 | 3 | 1 |
| 4. State Management | 5 | ~60 | 2 | 3 | 0 |
| 5. Permissions Checks | 4 | ~70 | 1 | 2 | 1 |
| 6. Data Fetching | 8 | ~70 | 5 | 3 | 0 |
| 7. Modal Systems | 6 | ~25 | 2 | 3 | 1 |
| 8. Drawer Systems | 4 | ~8 | 3 | 1 | 0 |
| 9. Tab Systems | 5 | ~20 | 3 | 1 | 1 |
| 10. Table Systems | 7 | ~90 | 4 | 2 | 1 |
| **TOTAL** | **60** | **~180 unique files** | **30** | **22** | **8** |

- **Total distinct duplication findings**: 60
- **Files carrying at least one duplication**: ~180 of ~250 source files audited (**~72%**)
- **Already consolidated (clean)**: `can()`/`load_grants` RBAC engine · `workflow.emit()` (4 intentional exceptions) · `next_reference_number()` (1 exception) · `money(luma)` admin · `timeAgo()` · `humanizeStatus()` · Stripe webhook verification · `IdempotencyMiddleware` · `PageShell` (51/55 views) · `Toast`/`toastHost` · `KPITile` primitive · `useFocusTrap` in `Overlay`/`Modal` · `_assert_production_deploy_contract()` · production deploy contract · `access.py can()` RBAC
- **% of surface already consolidated**: ~28% of audited files are clean
- **% with at least one duplication**: ~72%

---

## Ranked by Maintenance Cost — Top 20 (80/20 list)

The following 20 actions eliminate the highest concentration of duplication with the smallest per-file effort. Most are mechanical (grep-and-replace or codemod). Combined they touch ~2,100 of the ~3,400 individual occurrence-equivalents from the tokenization audit.

| Rank | Finding | Action | Files fixed | Occurrences eliminated | Effort |
|---|---|---|---|---:|---|
| 1 | **DF-8** No OpenAPI-to-TS codegen | Adopt `openapi-typescript`; generate types from OpenAPI schema | 60+ type files | Eliminates entire hand-mirror maintenance burden | L |
| 2 | **DF-3** Pagination hand-rolled in 12+ routers | Replace with `pagination.py Page.from_request().apply(q)` | 12+ | ~25 `LIMIT/OFFSET` blocks | S |
| 3 | **BL-1** Balance-due missing credit-note deduction | Replace 5 callers with `outstanding_for_invoice(s, inv.id)` | 5 | 5 (but closes a **customer-visible financial bug**) | S |
| 4 | **BL-5** `_parse_dt` tz-naive bug in 16+ routers | Export to `app/utils/dt.py`; replace 16 inline copies | 17 | 16+ copies + eliminates latent `TypeError` | S |
| 5 | **AC-1** `authH` defined 32 times privately | Import from `lib/billing.ts` everywhere | 32 | 30 local declarations | S |
| 6 | **DF-1/DF-2** `useEffect+fetch+setState` + `alive` guard (78 pairs) | Extract `useFetch<T>(url, token)` hook; adopt across all views | ~35 | ~78 `alive` guard pairs + all loading/error states | M |
| 7 | **SM-1** `token` prop-drilled to 55+ views | Move to `AuthContext` or Zustand store | 55+ | Removes entire prop chain | M |
| 8 | **TL-6 + TL-7** `_ensure_user` × 18 + `_customer` × 27 test fixtures | Move both to `conftest.py` | 45 | 45 duplicated function bodies | S |
| 9 | **TB-4** 9-tab spec: 16 duplicate React tab body components | Parameterize `views/customer-tabs/*`; delete 16 dupes | 3 | 16 duplicate React functions, ~600 LOC | M |
| 10 | **DR-1 + MO-1** Studio `DrawerShell` × 3 + `CreateDialog` × 3 | Build `<StudioDrawer>` component wrapping `Overlay` | 6 | ~240 inline styles + no-ARIA fixed | M |
| 11 | **DF-4** `fmtDate` × 15 + **DF-5** `fmtDateTime` × 6 | Add both to `lib/time.ts`; remove 21 local definitions | 21 | 21 local function definitions | S |
| 12 | **AC-2** 57 raw `fetch()` in 18 views | Route all through `bget`/`bpost` | 18 | 57 raw fetch calls | M |
| 13 | **BL-2** `amd()` × 4 with inconsistent rounding | Extract to `app/utils/money.py`; standardize to 2 d.p. | 4 | 4 local defs + rounding bug | S |
| 14 | **BL-4** `communications.py` COUNT+1 race | Replace with `next_reference_number()` | 2 | 1 retry loop + race condition | S |
| 15 | **TL-4** Inline row actions in 15+ tables | Migrate to `<RowActionsMenu>` | 15+ | ~30 ad-hoc action buttons | M |
| 16 | **E3** `_login` × 21 test fixtures | Move to `conftest.py` | 21 | 21 local `_login` functions | S |
| 17 | **AC-3** No centralized 401 handler in admin | Add 401 intercept to `bget`/`bpost` layer | 1 lib file | closes silent expiry breakage | S |
| 18 | **TB-1** Build `<DetailTab>` + `<PillTab>` primitives | Replace 7 inline tab button implementations | 7 | 7 local tab components + keyboard nav added | M |
| 19 | **AC-5** 8 bare `httpx.AsyncClient()` calls | Create `http_client.py` factory | 5 | 8 instantiation sites | S |
| 20 | **SM-2** `fetchCapabilities` called in 5 views unnecessarily | Remove 5 redundant `fetchCapabilities` calls | 5 | 5 redundant network requests per mount | S |

**20 actions · ~180 files · removes ~78% of all duplication by occurrence count**

All 20 actions are surgical (no architectural rewrites required). Items 1 and 7 are the highest-leverage single investments: codegen eliminates an entire maintenance class; auth context eliminates a prop chain threading every view file in the application.

---

*This audit is a separate lens from the Production Cert chain (sealed `2c5edeb`) and the Tokenization Audit (`70fd44b`). No code was modified. Canonical implementations confirmed intact.*
